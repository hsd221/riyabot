[plugin]
enabled = true
config_version = "1.1.0"
# 必须填写允许执行同步命令的 QQ 号；留空时所有用户都无法触发。
permission = []

[sync]
# 使用 /qqemoji 或 /同步QQ表情 时未指定数量的默认值。
default_count = 10
# 单次同步硬上限不超过 1000。
max_count = 500
# 按目录内所有条目计数；待注册目录不再参与临时缓存批量清理。
max_pending = 1000
